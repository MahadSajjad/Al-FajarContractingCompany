# ----------------------------------------------------------------------------
# Supabase — contact form lead capture ONLY (insert-only table, RLS enforced).
# The anon key is safe to expose in the client given the insert-only policy.
# Copy this file to `.env` (local) and set the same vars in Vercel for prod.
# ----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
